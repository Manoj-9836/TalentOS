# TalentOS — Evaluation Metrics Report

Generated by `backend/scripts/run_evaluation.py`.

## 1. Retrieval quality

_Not available: FAISS index not found: F:\Gitprojects\TalentOS\backend\indices\candidate_index.faiss_

## 2. Ranking quality

_Not available: [Errno 2] No such file or directory: 'F:\\Gitprojects\\TalentOS\\backend\\data\\embeddings\\job_embeddings.pkl'_

## 3. Recruitability model (training-time metrics)

- MAE: **0.1445**
- RMSE: **0.1674**
- R²: **0.0443**

Feature importances (split count):

| Feature | Importance |
|---------|------------|
| growth_score | 574 |
| github_activity | 550 |
| learning_velocity | 520 |
| stability_score | 518 |
| skill_credibility | 500 |

> ⚠️ R² is low. This is the honest ceiling for an aggregate-feature-only
> LightGBM model on a behavioural target with limited within-class variance.
> Treat the recruitability component as a soft prior; the dominant signal
> is the BGE semantic-similarity layer. See `evaluation_report.md` for
> the target-comparison sweep that justifies the choice of target.

## Methodology

- **Retrieval**: BAAI/bge-large-en-v1.5 embeddings, IndexFlatIP, 1024-d normalized vectors.
- **Ranking**: weighted sum of six components (see `ranking_engine.DEFAULT_WEIGHTS`).
- **Acceptance proxy**: `recruiter_response_rate` from `redrob_signals`.
- **Reference ranking**: `backend/data/raw/sample_submission.csv` (top-100 reference).

## 4. Ablation: recruitability as a soft prior

The recruitability component is intentionally weighted at 0.25–0.30 of the
final rank score, with semantic similarity (BGE) carrying the dominant weight.
Ablation table — to be populated by re-running the ranking pipeline with the
recruitability weight zeroed out:

| Configuration                       | Pearson r | Spearman ρ |
|-------------------------------------|-----------|------------|
| Full ranking (recruitability = 0.25)| _tbd_     | _tbd_      |
| Ranking without recruitability      | _tbd_     | _tbd_      |
| Ranking without semantic (BGE)      | _tbd_     | _tbd_      |

Populate this table by calling `rank_candidates_from_dicts` from
`backend.ml.ranking.ranking_engine` with custom `RankingWeights` (see
`docs/architecture.png` for the component weights).
